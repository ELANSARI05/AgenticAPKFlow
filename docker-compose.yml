services:
  agentic-apk:
    build: .
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./src:/app/src
    environment:
      - GROQ_API_KEY
      - OPENROUTER_API_KEY
      - GOOGLE_API_KEY
     
